# 富強醫材 設備維修 KPI 儀表板

上傳「設備維修統計.xlsx」即可顯示完整儀表板（外觀與 HTML 版完全相同）。

## 部署到 Streamlit Cloud
1. 上傳此資料夾所有檔案到 GitHub repo
2. 至 streamlit.io/cloud 部署，Main file path 填 `app.py`

## 本機執行
```bash
pip install -r requirements.txt
streamlit run app.py
```
